{% extends "base-admin.html.twig" %}

{% block body %}
<main class=container>
	<h1 class="mb-5">{{ title }}</h1>

        <table class="table table-striped mt-3">
            <thead>
                <tr>
                    
                    <th>
                        #
                    </th>
                    <th>
                        Nom
                    </th>
                    <th>
                        Email
                    </th>
                    <th>
                        Téléphone
                    </th>
                    <th>
                        Nombre de personne
                    </th>
                    <th>
                        la date
                    </th>
                    <th>
                       L'heure
                    </th>
                    <th>
                        Message
                    </th>
                </tr>
            </thead>
            <tbody>
                 {% for reservation in reservations %}
        <tr>
           
                    <td>
                        {{ reservation.id }}
                    </td>
                    <td>
                        {{ reservation.name|escape }}
                    </td>
                    <td>
                        {{ reservation.email|escape }}
                    </td>
                    <td>
                        {{ reservation.telephone|escape }}
                    </td>
                    <td>
                        {{ reservation.number|escape }}
                    </td>
                    <td>
                        {{ reservation.date }}
                    </td>
                    <td>
                        {{ reservation.hour }}
                    </td>
                    <td>
                        {{ reservation.message|escape }}
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        
        </table>
</main>
{% endblock %}